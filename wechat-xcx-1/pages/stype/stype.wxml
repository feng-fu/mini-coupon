<!--pages/stype/stype.wxml-->

<view class="container">
  <view class="banner-container">
    <image class="s-banner" src="{{banner}}" wx:if="{{banner}}" mode="widthFix" />
  </view>
  <production productions="{{productions}}" />
</view>
